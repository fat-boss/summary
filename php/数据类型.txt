8种原始类型 
4中标量类型 boolean integer float/double string
复合类型 array object
特殊类型 resource null

无论声明还是调用变量 都要用$


字符串：三种定义方式 ，单引号、双引号、界定符(<<<)
双引号所包含的变量会自动被替换成实际数值，单引号中包含
的变量则按普通字符串输出

界定符同双引号作用相同

	echo <<<abc   //这里可以随便起个名字
	输出的字符串
abc;  //这里收尾，测试中必须向左置顶 不然会报错