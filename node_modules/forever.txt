可以避免node开启服务器后占用当前shell

npm install -g forever

forever start app.js //可以开启服务器而不占用当前shell
forever stop app.js //关闭

forever list // 可以看见日志的目录

forever -w app.js //这个随便还是占用当前shell  但是可以做到实时刷新 
 后台里面的文件（不仅app.js）改变保存后  可以自动重启服务器