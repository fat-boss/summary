var exress = require('express');
var app = express();
var server = require('http').createServer(app);
var io = require('socket.io').listen(server);


server.listen(8080,function(){}) //这里注意一定要是server去监听，否则如果是app去监听，前端页面无法引入socket.io.js